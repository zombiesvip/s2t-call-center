{
  "name": "s2t",
  "version": "1.0.0",
  "description": "Speech to text",
  "main": "index.js",
  "dependencies": {
    "@google-cloud/speech": "^2.1.1",
    "chokidar": "^2.0.4",
    "fs-extra": "^7.0.0",
    "get-audio-duration": "^0.0.1",
    "mongodb": "^3.1.7",
    "musicmetadata": "^2.0.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^10.0.1",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js",
    "audio_source": "npm run build && node dist/Utils/runOnSrcFolder.js",
    "db": "npm run build && node dist/createDB",
    "check_length": "npm run build && node dist/Utils/longAudioFilter.js"
  },
  "author": "",
  "license": "ISC"
}
